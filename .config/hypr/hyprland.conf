
# #######################################################################################
# AUTOGENERATED HYPR CONFIG.
# PLEASE USE THE CONFIG PROVIDED IN THE GIT REPO /examples/hypr.conf AND EDIT IT,
# OR EDIT THIS ONE ACCORDING TO THE WIKI INSTRUCTIONS.
# #######################################################################################

autogenerated = 0 # remove this line to remove the warning

# This is an example Hyprland config file.
# Refer to the wiki for more information.
# https://wiki.hyprland.org/Configuring/Configuring-Hyprland/

# Please note not all available settings / options are set here.
# For a full list, see the wiki

# You can split this configuration into multiple files
# Create your files separately and then link them to this file like this:
# source = ~/.config/hypr/myColors.conf


################
### MONITORS ###
################

# See https://wiki.hyprland.org/Configuring/Monitors/
monitor=,preferred,auto,auto
monitor = eDP-1, preferred, auto, 1 # This is for my laptop monitor. T580
#monitor = HDMI-A-2, 1920x1080@60, 1920x0, auto # TV
#monitor = HDMI-A-1, 1920x1080@60, 0x0,auto # Samsung

# workspace = 1, monitor:HDMI-A-1
# workspace = 2, monitor:HDMI-A-1
# workspace = 3, monitor:HDMI-A-1
# workspace = 4, monitor:HDMI-A-1
# workspace = 5, monitor:HDMI-A-1

###################
### MY PROGRAMS ###
###################

# See https://wiki.hyprland.org/Configuring/Keywords/

# Set programs that you use
$terminal = kitty
$fileManager = thunar
$menu = wofi --show drun -I -a -n -W 500 -H 376 -S

#################
### AUTOSTART ###
#################

# Autostart necessary processes (like notifications daemons, status bars, etc.)
# Or execute your favorite apps at launch like this:

# exec-once = $terminal
# This is for authentication agent
exec-once = systemctl --user start hyprpolkitagent
exec-once = /usr/lib/geoclue-2.0/demos/agent # This is for geoclue, which is used by some apps to get location data as zen browser does.
exec-once = nm-applet
exec-once = sleep 1 && dms run #hyprpanel #blueman-applet & swww-daemon & hypridle #swaync for notifications
exec-once = privateinternetaccess
exec-once = wl-paste --watch cliphist store & # This for clipboard manager acitive at startup.
#exec-once = hyprpaper
#exec-once = ~/.config/hypr/change_wallpaper.sh
#exec-once = wlsunset -s 18:00 -S 08:30 -t 2500 -T 4000

#Below is the command to set the environment variables for screensharing.
exec-once = dbus-update-activation-environment --systemd WAYLAND_DISPLAY XDG_CURRENT_DESKTOP

exec-once = [workspace special:magic silent] notion-app
windowrule = match:class Notion, workspace special:magic silent
exec-once = [workspace special:magic silent] pear-desktop
windowrule = match:class pear-desktop, workspace special:magic silent

exec-once = [workspace 1 silent] zen-browser # microsoft-edge-stable
# exec-once = [workspace 2 silent] obsidian
exec-once = [workspace 2 silent] $terminal --session ~/.config/kitty/nvim.conf

windowrule {
  name = discord
  match:initial_class = discord
  workspace = 3 silent
}

exec-once = discord

windowrule {
  name = steam
  match:initial_class = steam
  workspace = 3 silent
}

exec-once = steam

exec-once = [workspace 4 silent] $terminal zellij --layout ~/.config/zellij/juan.kdl options --simplified-ui true --no-pane-frames # This is for no ui in zellij.
# exec-once = [workspace 4 silent] $terminal btop
# exec-once = [workspace 4 silent] $terminal cava
# exec-once = [workspace 4 silent] $terminal lavat -s10 -r1 -b20 -p2
# exec-once = [workspace 4 silent] $terminal cmatrix -raco -u 10 -M Juan
# exec-once = [workspace 4 silent] $terminal pipes.sh

#############################
### ENVIRONMENT VARIABLES ###
#############################

# See https://wiki.hyprland.org/Configuring/Environment-variables/

env = ICON_THEME,candy-icons          # ↔ ~/.icons/default/index.theme
env = GTK_THEME,Arc-Dark              # ↔ ~/.config/gtk-3.0/settings.ini  
env = XCURSOR_THEME,Bibata-Modern-Classic # ↔ ~/.icons/default/index.theme
env = XCURSOR_SIZE,16
env = HYPRCURSOR_THEME,Bibata-Modern-Classic
env = HYPRCURSOR_SIZE,16

#####################
### LOOK AND FEEL ###
#####################

# Refer to https://wiki.hyprland.org/Configuring/Variables/

# https://wiki.hyprland.org/Configuring/Variables/#general
general { 
    gaps_in = 2
    gaps_out = 2

    border_size = 2

    # https://wiki.hyprland.org/Configuring/Variables/#variable-types for info about colors
    col.active_border = rgba(33ccffee) rgba(00ff99ee) 45deg
    col.inactive_border = rgba(595959aa)

    # Set to true enable resizing windows by clicking and dragging on borders and gaps
    resize_on_border = false 

    # Please see https://wiki.hyprland.org/Configuring/Tearing/ before you turn this on
    allow_tearing = false

    layout = master
}

# https://wiki.hyprland.org/Configuring/Variables/#decoration
decoration {
    rounding = 2

    # Change transparency of focused and unfocused windows
    active_opacity = 1.0
    inactive_opacity = 0.7

#    drop_shadow = true
#    shadow_range = 4
#    shadow_render_power = 3
#    col.shadow = rgba(1a1a1aee)

    # https://wiki.hyprland.org/Configuring/Variables/#blur
    blur {
        enabled = true
        size = 3
        passes = 1
        
        vibrancy = 0.1696
    }
}

# https://wiki.hyprland.org/Configuring/Variables/#animations
animations {
    enabled = true

    # Default animations, see https://wiki.hyprland.org/Configuring/Animations/ for more

    bezier = myBezier, 0.05, 0.9, 0.1, 1.05

    animation = windows, 1, 7, myBezier
    animation = windowsOut, 1, 7, default, popin 80%
    animation = border, 1, 10, default
    animation = borderangle, 1, 8, default
    animation = fade, 1, 7, default
    animation = workspaces, 1, 6, default
}

# See https://wiki.hyprland.org/Configuring/Dwindle-Layout/ for more
dwindle {
    pseudotile = true # Master switch for pseudotiling. Enabling is bound to mainMod + P in the keybinds section below
    preserve_split = true # You probably want this
}

# See https://wiki.hyprland.org/Configuring/Master-Layout/ for more
master {
}

# https://wiki.hyprland.org/Configuring/Variables/#misc
misc { 
    force_default_wallpaper = -1 # Set to 0 or 1 to disable the anime mascot wallpapers
    disable_hyprland_logo = false # If true disables the random hyprland logo / anime girl background. :(
}


#############
### INPUT ###
#############

# https://wiki.hyprland.org/Configuring/Variables/#input
input {
    kb_layout = us, us
    kb_variant = intl, colemak
    kb_model =
    kb_options = grp:win_space_toggle, caps:escape
    kb_rules =

    follow_mouse = 1 #Change 1 to follow focus of the mouse.

    sensitivity = 0 # -1.0 - 1.0, 0 means no modification.

    touchpad {
        natural_scroll = true
    }
}

# https://wiki.hyprland.org/Configuring/Variables/#gestures
gestures {
    gesture = 3, horizontal, workspace,
}

# Example per-device config
# See https://wiki.hyprland.org/Configuring/Keywords/#per-device-input-configs for more
device {
    name = epic-mouse-v1
    sensitivity = -0.5
}


####################
### KEYBINDINGSS ###
####################

# See https://wiki.hyprland.org/Configuring/Keywords/
$mainMod = SUPER # Sets "Windows" key as main modifier

# Example binds, see https://wiki.hyprland.org/Configuring/Binds/ for more
bind = $mainMod, RETURN, exec, $terminal
bind = $mainMod, V, togglefloating, # Alternate floating window.
bind = $mainMod, B, togglesplit, # Alternate split window. (Dwindle)
bind = $mainMod, W, fullscreen, # Fullscreen window.

bind = $mainMod, 26, exec, $fileManager # E
# bind = $mainMod, X, exec, $menu

bind = $mainMod, Z, exec, emote
bind = $mainMod, 58, exec, dms ipc call spotlight toggle # This is for the DMS launcher with letter m.

bind = $mainMod, 24, killactive, # Q
# bind = $mainMod, M, exit,
bind = $mainMod, 54, exec, hyprlock # swaylock or hyprlock

# Command for moving workspaces with mainMod + U+I.
bind = $mainMod, 31, workspace, e-1
bind = $mainMod, 32, workspace, e+1
bind = $mainMod, P, pseudo, # dwindle

# Comando para cambiar el wallpaper con hyprpaper.
#bind = $mainMod, I, exec, ~/.config/hypr/change_wallpaper.sh

# Move focus with mainMod
bind = $mainMod, 45, movefocus, l # J
bind = $mainMod, 46, movefocus, r # K
bind = $mainMod, 44, movefocus, u # L
bind = $mainMod, 47, movefocus, d # : o Ñ

# Switch workspaces with mainMod + [0-9]
bind = $mainMod, 1, workspace, 1
bind = $mainMod, 2, workspace, 2
bind = $mainMod, 3, workspace, 3
bind = $mainMod, 4, workspace, 4
bind = $mainMod, 5, workspace, 5
bind = $mainMod, 6, workspace, 6
bind = $mainMod, 7, workspace, 7
bind = $mainMod, 8, workspace, 8
bind = $mainMod, 9, workspace, 9
bind = $mainMod, 0, workspace, 10

# Move active window to a workspace with mainMod + SHIFT + [0-9]
bind = $mainMod SHIFT, 1, movetoworkspace, 1
bind = $mainMod SHIFT, 2, movetoworkspace, 2
bind = $mainMod SHIFT, 3, movetoworkspace, 3
bind = $mainMod SHIFT, 4, movetoworkspace, 4
bind = $mainMod SHIFT, 5, movetoworkspace, 5
bind = $mainMod SHIFT, 6, movetoworkspace, 6
bind = $mainMod SHIFT, 7, movetoworkspace, 7
bind = $mainMod SHIFT, 8, movetoworkspace, 8
bind = $mainMod SHIFT, 9, movetoworkspace, 9
bind = $mainMod SHIFT, 0, movetoworkspace, 10

# Keys for movetoworkspace
bind = $mainMod alt, 45, movewindow, l
bind = $mainMod alt, 46, movewindow, r 
bind = $mainMod alt, 44, movewindow, u 
bind = $mainMod alt, 47, movewindow, d 

# Keys for resize the window
bind = $mainMod CTRL, left, resizeactive, -50 0
bind = $mainMod CTRL, right, resizeactive, 50 0
bind = $mainMod CTRL, up, resizeactive, 0 -50
bind = $mainMod CTRL, down, resizeactive, 0 50

# Example special workspace (scratchpad)
bind = $mainMod, F, togglespecialworkspace, magic
bind = $mainMod SHIFT, F, movetoworkspace, special:magic

# Scroll through existing workspaces with mainMod + scroll
bind = $mainMod, mouse_down, workspace, e-1
bind = $mainMod, mouse_up, workspace, e+1

# Move/resize windows with mainMod + LMB/RMB and dragging
bindm = $mainMod, mouse:272, movewindow
bindm = $mainMod, mouse:273, resizewindow

# Fn keys for headphones commands
bind = , xF86AudioPlay, exec, playerctl play-pause
bind = , xF86AudioPause, exec, playerctl pause
bind = , xF86AudioNext, exec, playerctl next
bind = , xF86AudioPrev, exec, playerctl previous
bind = , xF86AudioMicMute, exec, pactl set-source-mute @DEFAULT_SOURCE@ toggle

# Brightness buttons
bind = , xF86MonBrightnessUp, exec, brightnessctl -q s +10%
bind = , xF86MonBrightnessDown, exec, brightnessctl -q s 10%-

# Volume and Media Control
bind = , XF86AudioRaiseVolume, exec, pactl set-sink-volume @DEFAULT_SINK@ +5%
bind = , XF86AudioLowerVolume, exec, pactl set-sink-volume @DEFAULT_SINK@ -5%
bind = , XF86AudioMicMute, exec, pactl set-source-mute @DEFAULT_SOURCE@ toggle
bind = , XF86AudioMute, exec, wpctl set-mute @DEFAULT_AUDIO_SINK@ toggle

plugin {
    hyprscrolling {
        column_width = 0.7
        fullscreen_on_one_column = true
        focus_fit_method = 1
        follow_focus = true
    }
}

bind = $mainMod, period, layoutmsg, move +col
bind = $mainMod, comma, layoutmsg, move -col
bind = $mainMod SHIFT, period, layoutmsg, movewindowto r
bind = $mainMod SHIFT, comma, layoutmsg, movewindowto l
bind = $mainMod SHIFT, up, layoutmsg, movewindowto u
bind = $mainMod SHIFT, down, layoutmsg, movewindowto d

# Screenshot a window
bind = CTRL ALT, PRINT, exec, HYPRSHOT_DIR="$HOME/Pictures" hyprshot -m window
# Screenshot a monitor
bind = , PRINT, exec, HYPRSHOT_DIR="$HOME/Pictures" hyprshot -m output
# Screenshot a region
bind = SHIFT, PRINT, exec, HYPRSHOT_DIR="$HOME/Pictures" hyprshot -m region
#You can also skip saving the screenshot to a file, copying it only to the clipboard:
bind = ALT, PRINT, exec, HYPRSHOT_DIR="$HOME/Pictures" hyprshot -m output --clipboard-only

##############################
### WINDOWS AND WORKSPACES ###
##############################

# See https://wiki.hyprland.org/Configuring/Window-Rules/ for more
# See https://wiki.hyprland.org/Configuring/Workspace-Rules/ for workspace rules

# Example windowrule v1
# windowrule = float, ^(kitty)$

# Example windowrule v2
# windowrulev2 = float,class:^(kitty)$,title:^(kitty)$

#windowrulev2 = suppressevent maximize, class:.* # You'll probably like this.
windowrule = match:class zen-browser, workspace 1 silent
windowrule = match:class discord, workspace 3 silent
windowrule = match:class steam, workspace 3 silent

# Reglas para ventanas flotantes y tamaño
windowrule = match:class $fileManager, float on, #size 900 600
windowrule = match:class org.pulseaudio.pavucontrol, float on
windowrule = match:class org.kde.gwenview, float on

# Regla para suprimir eventos y posición
windowrule = match:class kitty match:title btop, move 0 0
